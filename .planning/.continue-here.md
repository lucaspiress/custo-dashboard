---
context: spec-kit-redesign
phase: 3
task: T011-T014
total_tasks: 60
status: paused_pending_gate
last_updated: 2026-08-26
---

# Checkpoint — Custo Dashboard

## Estado atual

O planejamento Spec Kit da feature `001-redesign-custo-dashboard` está completo, analisado e aprovado. As Fases 1 e 2 passaram por gates técnicos. A Fase 3 implementou as rotas canônicas e o shell pós-login, mas ainda precisa do Gate 3 (revisão visual/arquitetural e smoke manual) antes de iniciar T015.

## Trabalho concluído

- T001–T004: fixtures de projeto e testes baseline.
- T005–T009: `routes.ts`, `project-context.ts` com cancelamento seguro e compatibilidade de API, inclusive Usuários admin.
- T010–T014: rotas canônicas, redirects legados, rail/barra de contexto e navegação de projetos.
- Commit existente da implementação visual: `18868cf`.
- Validação mais recente: `npm run test` = 37 testes aprovados; `npm run build` = aprovado; backend pytest = 148 testes aprovados (um aviso conhecido de depreciação Starlette/httpx).

## Decisões não negociáveis

- `frontend/src/pages/LoginPage.tsx` não pode ser alterada.
- O projeto ativo vem somente de `:id`; não criar seleção global persistida.
- Reutilizar abas existentes de dashboard; a rota seleciona a área.
- Não mudar API, autenticação, schema, persistência ou cálculo backend.
- Importação por linha e análise filtrada server-side são features futuras/pré-requisitos; não prometer ou simular esse comportamento.
- Playwright permanece trabalho futuro não acionável.

## Próxima ação

1. Ler, nesta ordem: `specs/001-redesign-custo-dashboard/spec.md`, `design.md`, `plan.md`, `tasks.md`, `.slim/deepwork/post-login-redesign.md`.
2. Executar Gate 3: revisar `App.tsx`, `AppShell.tsx`, `ProjetoContextBar.tsx` e `ProjetosPage.tsx`; rodar testes/build e smoke manual das rotas canônicas/legadas.
3. Corrigir somente achados materiais, fazer o gate passar e iniciar T015.

## Prompt de retomada

`Retome o projeto em C:\Users\assistentesolucoes\Desktop\custo-dashboard. Leia .planning/.continue-here.md e .planning/HANDOFF.json, siga os artefatos em specs/001-redesign-custo-dashboard e continue pelo Gate 3 da Fase 3. Preserve todas as decisões e limites listados no checkpoint.`

## Infraestrutura

- Frontend: Vite/React, comandos em `frontend/`.
- Backend: FastAPI/pytest, usar `$env:DATABASE_URL=""; ..\.venv\Scripts\python -m pytest -q` em `backend/`.
- Deploy: Vercel CLI disponível; publicar somente depois de testes/build passarem.
